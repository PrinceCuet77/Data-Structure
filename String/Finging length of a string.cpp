#include <bits/stdc++.h>
using namespace std;

int main()
{
        char a[] = "prince";

        int len = 0;
        for ( int i = 0; a[i] != '\0'; i++ )
                len++;

        cout << "Length of " << a << " is : " << len << endl;

        return 0;
}
